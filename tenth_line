# Read from the file file.txt and output the tenth line to stdout.
#!/bin/bash
# Check if file.txt has at least 10 lines, then print the 10th line
if [ $(wc -l < file.txt) -ge 10 ]; then
  head -n 10 file.txt | tail -n 1
fi
